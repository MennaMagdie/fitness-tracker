#This is just a dummy file so that we maintain the structure

# 'components' Folder: 
    # will contain reusable components (that we may use in different pages)
    # Landing (folder inside components): will contain components that are only used in Landing Page & their css
    # Home (folder inside components): will contain components that are only used in Home Page & their css
    # etc..

# 'pages' Folder:
    # will contain tsx files for all used pages

